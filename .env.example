# Models to pre-pull on first `docker compose up`.
# Space-separated. Add more (e.g. nomic-embed-text) as you need them.
MODELS=qwen2.5-coder:7b

# Model name advertised to ruflo when it asks for a default.
RUFLO_DEFAULT_MODEL=qwen2.5-coder:7b
